# 📘 الدليل الشامل والموسوعة الكاملة لمميزات مشروع SnipGlide Python Pro

<div align="center">

![SnipGlide Banner](https://raw.githubusercontent.com/mahmoud-gala-dev/SnipGlide/main/snipglide/assets/icon.ico)

### **SnipGlide Python Pro**
**المساعد الذكي المتكامل للإنتاجية، توسيع النصوص، إدارة الحافظة، وتصوير وقص وتسجيل الشاشة فيديو مع تنظيم متطور بالمجلدات والسحب والإفلات.**

[![Python Version](https://img.shields.io/badge/Python-3.11%20%7C%203.12%20%7C%203.14-blue?style=for-the-badge&logo=python)](https://www.python.org/)
[![GUI Framework](https://img.shields.io/badge/PySide6-Qt6-green?style=for-the-badge&logo=qt)](https://www.qt.io/)
[![Database](https://img.shields.io/badge/SQLite-WAL%20Mode-003B57?style=for-the-badge&logo=sqlite)](https://www.sqlite.org/)
[![Platform](https://img.shields.io/badge/Platform-Windows%2010%20%2F%2011-0078D6?style=for-the-badge&logo=windows)](https://microsoft.com)
[![Privacy](https://img.shields.io/badge/Privacy-100%25%20Local%20First-success?style=for-the-badge)](https://github.com/mahmoud-gala-dev/SnipGlide)
[![License](https://img.shields.io/badge/License-MIT-purple?style=for-the-badge)](LICENSE)

</div>

---

## 📑 فهرس المحتويات

1. [نظرة عامة وفلسفة المشروع](#-1-نظرة-عامة-وفلسفة-المشروع)
2. [محرك التوسيع النصي الذكي (Text Expansion Engine)](#-2-محرك-التوسيع-النصي-الذكي-text-expansion-engine)
3. [نظام التقاط وقص وتسجيل الشاشة بالفيديو (Screen Capture & Video Recording)](#-3-نظام-التقاط-وقص-وتسجيل-الشاشة-بالفيديو-screen-capture--video-recording)
4. [تنظيم الصور والفيديوهات، المجلدات، والسحب والإفلات (Gallery & Organization)](#-4-تنظيم-الصور-والفيديوهات-المجلدات-والسحب-والإفلات-gallery--organization)
5. [المفكرة الذكية ومحرر الأكواد المتقدم (Smart Notepad & Code Editor)](#-5-المفكرة-الذكية-ومحرر-الأكواد-المتقدم-smart-notepad--code-editor)
6. [إدارة الملاحظات السريعة والملاحظات التفاعلية (Notes & Chat Notes)](#-6-إدارة-الملاحظات-السريعة-والملاحظات-التفاعلية-notes--chat-notes)
7. [مدير سجل الحافظة الذكي (Clipboard History Manager)](#-7-مدير-سجل-الحافظة-الذكي-clipboard-history-manager)
8. [المساعد الذكي وأدوات الإنتاجية (AI Assistant & Tools)](#-8-المساعد-الذكي-وأدوات-الإنتاجية-ai-assistant--tools)
9. [الأمان والخصوصية وصيانة النظام (Security, Privacy & Health)](#-9-الأمان-والخصوصية-وصيانة-النظام-security-privacy--health)
10. [جدول اختصارات لوحة المفاتيح العامة (Global Hotkeys)](#-10-جدول-اختصارات-لوحة-المفاتيح-العامة-global-hotkeys)
11. [البنية المعمارية للمشروع (Architecture & Codebase)](#-11-البنية-المعمارية-للمشروع-architecture--codebase)
12. [دليل التثبيت والتشغيل وبناء النسخة التنفيذية (Installation & Build)](#-12-دليل-التثبيت-والتشغيل-وبناء-النسخة-التنفيذية-installation--build)
13. [حزمة أدوات المطورين المتكاملة (Developer Productivity Suite — Phases 1–8)](#-13-حزمة-أدوات-المطورين-المتكاملة-developer-productivity-suite--phases-18)

---


## 🌟 1. نظرة عامة وفلسفة المشروع

تم بناء وتطوير **SnipGlide Python Pro** ليكون بيئة العمل المكتبية الأكثر تكاملاً وسرعة لمستخدمي نظام Windows من محترفين، مبرمجين، كتاب، وصناع محتوى.

### المبادئ الأساسية للمشروع:
- **الخصوصية المطلقة (100% Local-First):** لا تخرج أي معلومة من جهازك؛ كل شيء محفوظ محلياً في قاعدة بيانات SQLite مشفرة ومحسنة تحت مجلد `%APPDATA%\SnipGlidePythonPro`.
- **الخفة والسرعة الفائقة:** استخدام خيوط معالجة منفصلة (`QThread` و `ThreadPool`) لجميع العمليات الثقيلة (التسجيل، الكاش، الفلاتر) لضمان بقاء الواجهة تعمل بمعدل 60 إطار/ثانية دون أي تهنيج.
- **التصميم العصري الداكن (Modern Glassmorphism & Dark UI):** واجهة استخدام استثنائية مبنية بـ PySide6 مع تدرجات لونية، خطوط واضحة، وأيقونات دلالية معمارية.

---

## ⚡ 2. محرك التوسيع النصي الذكي (Text Expansion Engine)

يعمل محرك التوسيع النصي في الخلفية بشكل صامت ويراقب ضغطات لوحة المفاتيح عبر النظام بالكامل (Global Keyboard Listener).

```mermaid
flowchart LR
    A[ضغطات المفاتيح في أي برنامج] --> B[محرك الاستماع Global Listener]
    B --> C{مطابقة المحفز Trigger?}
    C -->|نعم| D[فحص قيود التطبيق Window Filter]
    D -->|مطابق| E[توليد المتغيرات والتاريخ والوقت]
    E --> F[محو الاختصار وإدخال النص البديل فوراً]
    C -->|لا| G[تجاهل]
```

### أبرز مميزات المحرك:
1. **المحفزات المرنة (Flexible Triggers):**
   - يدعم أي تركيبة من الحروف والرموز مثل: `#sig`، `$email`، `!date`، `@@tel`، أو حتى كلمات عادية بدون أي قيود.
2. **المتغيرات الديناميكية المضمنة (Dynamic Placeholders):**
   - استبدال تلقائي بالوقت والتاريخ: `{date}`، `{time}`، `{datetime}`.
   - دمج محتوى الحافظة الحالي: `{clipboard}`.
   - تحديد موضع مؤشر الكتابة بعد التوسيع: `{cursor}`.
3. **دعم التعابير النمطية (Regex Enabled):**
   - إمكانية تفعيل التعابير النمطية لاكتشاف الأنماط المعقدة وتوليد ردود ونصوص ديناميكية.
4. **تصفية النوافذ والتطبيقات (Window & App Filter):**
   - حصر عمل اختصار معين فقط في تطبيقات محددة (مثل: جعله يعمل في `Visual Studio Code` أو `Google Chrome` فقط دون باقي البرامج).
5. **التنظيم بالمجموعات والألوان (Groups & Categories):**
   - تقسيم القصاصات إلى مجموعات ملونة (مثل: العمل، البرمجة، الردود السريعة، الشخصية) مع إمكانية طي وفرد المجموعات.
6. **الاستيراد والتصدير عبر Excel (`.xlsx`):**
   - تصدير كامل القصاصات بضغطة زر لملف إكسل، أو استيراد مئات القصاصات دفعة واحدة مع وجود قالب جاهز للتحميل.
7. **التحكم الديناميكي والإحصائيات:**
   - تفعيل/تعطيل كل قصاصة على حدة، عداد مرات الاستخدام لمعرفة أكثر النصوص استهلاكاً، وزر التفضيل ⭐.

---

## 🎥 3. نظام التقاط وقص وتسجيل الشاشة بالفيديو (Screen Capture & Video Recording)

يقدم SnipGlide نظاماً شاملاً للقطات الشاشة وتسجيلات الفيديو لا يحتاج إلى أي برامج خارجية كـ OBS أو Snipping Tool:

### أ) لقطات الشاشة (Screenshots):
- **التقاط الشاشة بالكامل فوراً:** بالضغط على اختصار `Ctrl + PrintScreen` من أي مكان.
- **تحديد واقتصاص مساحة مخصصة (Area Snipping):** بالضغط على اختصار `Win + PrintScreen`، تظهر طبقة تظليل شفافة تسمح بسحب مستطيل بدقة بكسلية عالية مع شاشة أبعاد وإجراءات فورية.
- **عارض الصور المدمج (`ScreenshotViewerDialog`):**
  - فحص وتكبير/تصغير لقطة الشاشة (`Zoom in/out`, `Fit to window`, `1:1`).
  - نسخ الصورة فوراً للحافظة بصيغة Pixmap جاهزة للصق في واتساب أو تيليجرام أو المستندات.
  - خيار حفظ باسم (`Save As`)، أو كشف مسار الملف في مستكشف ويندوز.

### ب) تسجيل الفيديو عالي الدقة (Screen Video Recording):
- **تسجيل فيديو لكامل الشاشة (Full-Screen MP4):** تصوير فيديو سلس بمعدل 24-30 إطار في الثانية بدقة الشاشة الكاملة.
- **تسجيل مساحة مقتطعة محددة (Area Video MP4):** أداة تفاعلية (`VideoAreaOverlayWidget`) لتحديد نافذة أو جزء مخصص من الشاشة قبل بدء التسجيل مع إطار مضيء متحرك يوضح حدود منطقة التسجيل.
- **شريط التحكم العائم الذكي (`ScreenRecorderFloatingWidget`):**
  - ويدجت عائم يظهر في أسفل الشاشة (أو يسحب لأي مكان تريده).
  - وميض أحمر حي مع مؤقت زمني دقيق: `🔴 REC 00:15`.
  - زر إيقاف مؤقت واستئناف التسجيل (`⏸️` / `▶️`).
  - زر إنهاء وحفظ التسجيل تلقائياً في قاعدة البيانات (`⏹️ إنهاء وحفظ`).
  - زر إلغاء وحذف الملف المؤقت فوراً دون حفظ (`❌`).
- **إبراز مؤشر الفأرة (Mouse Pointer Highlight):** تمييز حركة الفأرة تلقائياً داخل الفيديو ليسهل متابعة الشروحات.
- **توليد المصغرات التلقائي (Automatic Thumbnails):** استخراج صورة مصغرة تلقائياً من أول إطار للفيديو وعرضها كغلاف في المعرض.
- **مشغل الفيديو المدمج الاحترافي (`VideoPlayerDialog`):**
  - تشغيل فوري داخل التطبيق دون الحاجة لفتح برامج تشغيل خارجية.
  - شريط تقديم وترجيع تفاعلي (`Seek Slider`) للتحكم في اللحظة الزمنية للمشاهدة.
  - شريط مستوى الصوت وزر الكتم الصوتي.
  - زر تشغيل خارجي بالمشغل الافتراضي لنظام ويندوز وزر حذف وحفظ ومسار الملف.

---

## 🗂️ 4. تنظيم الصور والفيديوهات، المجلدات، والسحب والإفلات (Gallery & Organization)

تتميز صفحة لقطات وتسجيلات الشاشة بنظام تنظيمي متقدم لا يوجد في التطبيقات المماثلة:

### أ) طرق العرض الثلاث القابلة للتبديل (3 Switchable View Modes):
1. **⊞ عرض البطاقات (Large Cards Grid):**
   - شبكة أنيقة بثلاثة أعمدة تعرض مصغرات عريضة للقطات والفيديوهات مع علامات الوقت، الأبعاد، الحجم، وأزرار التحكم السريع.
2. **▦ عرض المصغرات المكثفة (Compact Grid):**
   - شبكة مكثفة بستة أعمدة (6 Columns) بعناصر مدمجة (170×190px) تتيح تصفح واستعراض مئات الملفات في مساحة واحدة بسهولة متناهية.
3. **☰ عرض القائمة والجدول (Detailed List View):**
   - صفوف أفقية مدمجة بارتفاع 54px تتضمن مصغر المعاينة، اسم الملف، نوع اللقطة، شارة المجلد، الأبعاد والمدة، حجم الملف، التاريخ، وزر الإجراءات.

### ب) نظام المجلدات الذكي (Folders System):
- مجلدات افتراضية مسبقة: **العامة**، **العمل**، **مشاريع**، **شروحات**.
- زر `➕ مجلد جديد` لإنشاء مجلدات مخصصة بأسماء وألوان مميزة.
- حساب حي لعدد الملفات بداخل كل مجلد يظهر في شريط التبويبات العلوي.
- النقر بالزر الأيمن للفأرة على أي مجلد مخصص لإعادة تسميته ✏️ أو حذفه 🗑️ مع ضمان حماية ملفاتك بترحيلها تلقائياً إلى مجلد "العامة".

### ج) السحب والإفلات وقائمة النقل السريع (Drag & Drop & Quick Move):
- **السحب والإفلات التفاعلي:** اسحب أي بطاقة صورة أو فيديو بزر الفأرة الأيسر وأفلتها مباشرة فوق زر المجلد بالأعلى.
- **التوهج البصري (Glow Animation):** عند مرور العنصر فوق المجلد المستهدف، يتوهج زر المجلد بإطار أزرق متقطع ومضيء لتأكيد استقبال الإفلات.
- **قائمة النقل السريع:** زر `📂 نقل` متوفر على كل بطاقة وصف يفتح قائمة منسدلة فورية بجميع المجلدات لنقل الملف بنقرة واحدة بدون سحب.

### د) نظام تعدد الصفحات (Pagination Bar):
- شريط تحكم كامل في أسفل المعرض:
  - مؤشر نطاق العرض: `عرض 1 - 24 من أصل 85 عنصر`.
  - أزرار التنقل السريع: `⏮ الأولى`، `◀ السابق`، مؤشر رقم الصفحة الحالية، `التالي ▶`، و `الأخيرة ⏭`.
  - محدد عدد العناصر في الصفحة: `12`، `24`، `48`، `96`، أو `الكل`.

---

## 📝 5. المفكرة الذكية ومحرر الأكواد المتقدم (Smart Notepad & Code Editor)

بيئة كتابة وتحرير فائقة المرونة تخدم النصوص العادية والأكواد البرمجية:

- **نظام التبويبات المتعددة (Multi-tab Environment):** فتح عدة ملفات ومستندات في نفس الوقت والتنقل السلس بينها.
- **محرر أكواد مدمج مع أرقام الأسطر (Line Numbers & Code Syntax):**
  - شريط أرقام الأسطر الجانبي المتزامن مع التمرير.
  - تلوين النصوص البرمجية وتمييز الكلمات المفتاحية لمختلف لغات البرمجة (Python, JavaScript, HTML, CSS, JSON, SQL, Plain Text).
- **دعم اللغة العربية الكامل (Full RTL Support):**
  - دعم الكتابة من اليمين إلى اليسار للنصوص والمستندات العربية بسلاسة تامة.
- **تخصيص العرض والخطوط:**
  - إمكانية تكبير وتصغير الخط وتغيير تباعد الأسطر وحفظ التفضيلات تلقائياً.
- **حفظ واستعادة الجلسات التلقائي (Session Auto-Restore):**
  - عند إعادة فتح التطبيق، يستعيد المحرر تلقائياً كافة الملفات والتبويبات غير المحفوظة دون ضياع أي كلمة.

---

## 💬 6. إدارة الملاحظات السريعة والملاحظات التفاعلية (Notes & Chat Notes)

- **صفحة الملاحظات الكلاسيكية (`NotesPage`):**
  - بطاقات ملاحظات سريعة مع إمكانية البحث الفوري والنسخ بنقرة واحدة إلى الحافظة.
  - تصنيف الملاحظات وتثبيت الملاحظات الهامة بالأعلى (`Pinned Notes`).
- **نظام Chat Notes الثوري:**
  - واجهة تشبه تطبيقات المحادثة الفورية لكتابة الخواطر، الأفكار، والمهام كرسائل في جدول زمني مرتب.
  - دعم الملاحظات الصوتية مع مشغل صوتي مدمج (`VoicePlayerWidget`) لتشغيل التسجيلات الصوتية مباشرة داخل الملاحظة.
- **رأس المحادثة العائم (`FloatingChatHead`):**
  - أيقونة دائرية عائمة تظهر فوق كافة النوافذ تمكنك من فتح نافذة تدوين سريعة في أي لحظة دون مقاطعة عملك.

---

## 📋 7. مدير سجل الحافظة الذكي (Clipboard History Manager)

- **مراقبة الحافظة الخلفية (Background Clipboard Monitor):**
  - تسجيل تلقائي وفوري لكافة النصوص والروابط والأكواد المنسوخة عبر النظام.
- **تصفية وحماية البيانات:**
  - منع تكرار النصوص المتطابقة وترتيب السجل من الأحدث للأقدم.
  - كشف النصوص والبيانات الحساسة (مثل كلمات المرور والمفاتيح السرية).
- **البحث والصفحات:**
  - تصفح سجل الحافظة عبر صفحات منظمة مع بحث فوري سريع، وزر لحذف عنصر معين أو إفراغ السجل بالكامل.

---

## 🤖 8. المساعد الذكي وأدوات الإنتاجية (AI Assistant & Tools)

- **المساعد الذكي المدمج (`AIAssistantPage`):**
  - تكامل مرن مع خدمات الذكاء الاصطناعي (OpenAI, Google Gemini, أو أي خادم محلي متوافق مع API).
  - إمكانية تلخيص النصوص الطويلة، إعادة الصياغة، الترجمة الفورية، وتصحيح الأكواد.
- **لوحة الأوامر السريعة (`Command Palette`):**
  - نافذة منبثقة سريعة (تشبه لوحة أوامر VS Code و Sublime) للبحث عن أي وظيفة أو صفحة في البرنامج وتنفيذها بالضغط على المفاتيح.
- **أدوات مطوري الويب وقوالب البريد (`Web Dev & Email Dialogs`):**
  - أدوات مساعدة سريعة لمطوري الويب وتنسيق الألوان والأكواد، بالإضافة إلى قوالب بريد إلكتروني جاهزة للاستخدام الفوري.

---

## 🔒 9. الأمان والخصوصية وصيانة النظام (Security, Privacy & Health)

- **قفل الحماية بكلمة مرور رئيسية (Master Security PIN):**
  - إمكانية تعيين كلمة مرور أو رمز PIN لقفل التطبيق وحماية القصاصات والملاحظات الخاصة.
  - قفل تلقائي عند الخمول أو تقليل النافذة لحماية خصوصية المستخدم.
- **النسخ الاحتياطي والاستعادة (`Backup & Auto-Backup`):**
  - خيارات نسخ احتياطي فوري واسترجاع بكبسة زر لكافة البيانات.
  - مجدول نسخ احتياطي دوري للحفاظ على بياناتك من أي فقدان غير مقصود.
- **صيانة وتشخيص قاعدة البيانات (`Health & Maintenance`):**
  - أداة فحص سلامة قاعدة البيانات وتشغيل أوامر التحسين التلقائي (`VACUUM`, `WAL mode`) وتنظيف الملفات المؤقتة.

---

## ⌨️ 10. جدول اختصارات لوحة المفاتيح العامة (Global Hotkeys)

| الاختصار | الوظيفة |
|---|---|
| <kbd>Ctrl</kbd> + <kbd>PrintScreen</kbd> | التقاط الشاشة كاملة فوراً وحفظها في المعرض |
| <kbd>Win</kbd> + <kbd>PrintScreen</kbd> | فتح أداة تحديد واقتصاص جزء مخصص من الشاشة |
| <kbd>Ctrl</kbd> + <kbd>Alt</kbd> + <kbd>Shift</kbd> + <kbd>S</kbd> | إظهار / إخفاء نافذة برنامج SnipGlide من الخلفية |
| <kbd>Ctrl</kbd> + <kbd>B</kbd> | طي أو إظهار القائمة الجانبية (Drawer Sidebar) |
| <kbd>Ctrl</kbd> + <kbd>+</kbd> / <kbd>-</kbd> | تكبير أو تصغير حجم واجهة البرنامج |
| <kbd>Ctrl</kbd> + <kbd>0</kbd> | استعادة الحجم الافتراضي لواجهة البرنامج (100%) |

---

## 🏗️ 11. البنية المعمارية للمشروع (Architecture & Codebase)

```text
snipglide_python/
│
├── app.py                          # نقطة الانطلاق الرئيسية للتطبيق
├── requirements.txt                # حزم ومكتبات بايثون المطلوبة
├── SnipGlide.spec                  # ملف إعدادات بناء البرنامج لـ PyInstaller
├── test_video_recording_feature.py # حزمة اختبارات شاملة لتسجيل الفيديو
├── test_screenshot_organization.py # حزمة اختبارات شاملة للعرض والمجلدات والصفحات
│
└── snipglide/
    ├── core/                       # الإعدادات وثوابت المسارات
    │   └── config.py
    ├── database/                   # مستودعات قاعدة البيانات SQLite
    │   ├── connection.py           # الاتصال وجداول قاعدة البيانات وتهجرتها
    │   ├── screenshot_repo.py      # عمليات واستعلامات الصور والفيديوهات والمجلدات
    │   ├── snippet_repo.py         # مستودع الاختصارات والقصاصات
    │   ├── note_repo.py            # مستودع الملاحظات
    │   └── clipboard_repo.py       # مستودع سجل الحافظة
    ├── engine/                     # محرك الاستماع العالمي للوحة المفاتيح
    │   ├── listener.py
    │   ├── parser.py
    │   └── window_tracker.py
    ├── models/                     # نماذج الكائنات (Data Models)
    │   ├── screenshot.py
    │   ├── snippet.py
    │   └── note.py
    ├── services/                   # خدمات المنطق البرمجي في الخلفية
    │   ├── video_recording_service.py # خدمة تصوير الفيديو والترميز cv2
    │   ├── screenshot_service.py      # خدمة التقاط الصور
    │   ├── clipboard_monitor.py       # مراقبة الحافظة
    │   ├── security.py                # التشفير والأمان
    │   └── backup.py                  # النسخ الاحتياطي
    └── ui_qt/                      # واجهات المستخدم الاحترافية المبنية بـ PySide6
        ├── main_window.py          # النافذة الرئيسية والقوائم
        ├── sidebar.py              # القائمة الجانبية العصرية
        ├── screenshots_page.py     # صفحة المعرض، المجلدات، الصفحات، والسحب والإفلات
        ├── video_player_dialog.py  # مشغل الفيديو المدمج
        ├── video_area_overlay.py   # أداة تحديد مساحة الفيديو
        ├── recording_floating_widget.py # شريط التحكم العائم REC
        ├── notepad_page.py         # المفكرة الذكية ومحرر الأكواد
        ├── notes_page.py           # الملاحظات السريعة
        ├── chat_notes_page.py      # محادثة الملاحظات
        ├── clipboard_page.py       # سجل الحافظة
        └── tray_icon.py            # أيقونة صينية النظام بجوار الساعة
```

---

## 🚀 12. دليل التثبيت والتشغيل وبناء النسخة التنفيذية

### متطلبات التشغيل:
- نظام تشغيل: **Windows 10 أو Windows 11 (64-bit)**
- بايثون: **Python 3.11 أو أحدث**

### خطوات التشغيل من السورس كود:
```powershell
# 1. الدخول إلى مجلد المشروع
cd d:\snipglide_python

# 2. تفعيل البيئة الافتراضية
.\.venv\Scripts\Activate.ps1

# 3. تثبيت الاعتمادات
pip install -r requirements.txt

# 4. تشغيل التطبيق
python app.py
```

### بناء ملف تنفيذي مستقل (`SnipGlide.exe`):
لتحويل التطبيق إلى ملف تنفيذي يعمل على أي جهاز كمبيوتر دون الحاجة لتثبيت بايثون:
```powershell
pyinstaller SnipGlide.spec
```
الملف التنفيذي الناتج سيكون متاحاً بداخل مجلد `dist\SnipGlide.exe`.

---

## 🛠️ 13. حزمة أدوات المطورين المتكاملة (Developer Productivity Suite — Phases 1–8)

تم توسيع وتطوير SnipGlide ليصبح بيئة عمل وإنتاجية متكاملة لمهندسي ومطوري البرمجيات على ويندوز، متوافقة مع مبادئ Clean Architecture وعزل العمليات الثقيلة عن واجهة المستخدم (`QThread`):

### 📋 المرحلة 1: صندوق أدوات المطورين (Developer Toolbox)
- **JSON Formatter & Validator:** تنسيق، ضغط، التحقق، ترتيب المفاتيح، والهروب من الرموز.
- **Base64 & URL Tools:** تشفير وفك تشفير النصوص والروابط مع دعم كامل لترميز UTF-8.
- **JWT Decoder:** فك تشفير وفحص Header و Payload مع التحقق من صلاحية التوكن (Expiration).
- **UUID & Timestamp:** توليد معرفات UUID v4 فورية ومحول زمني بين Epoch والوقت البشري المحلي وUTC.
- **Hash Generator & Text Utils:** توليد بصمات MD5، SHA-1، SHA-256، والتحويل بين camelCase و snake_case و kebab-case وغيرها.

### 🔍 المرحلة 2: حقل تجارب الـ Regex ومراقب الحافظة المطور
- **Regex Playground:** مطابقة حية مع تمييز لوني للمطابقات والمجموعات (Capture Groups)، مع فحص ReDoS المسبق لمنع تجميد الواجهة.
- **Developer Clipboard:** تصنيف تلقائي وسريع لمحتويات الحافظة (JSON, SQL, URLs, Stack Traces, Emails, Code) مع حماية أمنية للبيانات الحساسة.

### ✂️ المرحلة 3: اختصارات الأكواد الذكية (Smart Code Snippets)
- دعم لغات البرمجة المتعددة مع التمييز اللوني.
- استبدال المتغيرات الديناميكية وتوليد نماذج الإدخال التفاعلية (`{{input:name}}`, `{{choice:options}}`).

### 🌐 المرحلة 4: مختبر واجهات برمجة التطبيقات (REST API Tester)
- **HTTP Methods:** دعم كامل لـ GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS.
- **محرر الطلبات:** محرر Key/Value لـ Headers و Query Parameters مع تفعيل وتعطيل لكل سطر.
- **Authentication:** دعم None, Bearer Token, Basic Auth, و API Key (Header أو Query Param).
- **عزل الخيوط (Thread Isolation):** تنفيذ جميع طلبات الشبكة في خيط `ApiRequestWorker` لمنع تجميد الواجهة مع معالجة كاملة للمهلات وأخطاء SSL/DNS.
- **تصدير وسجل آمن:** توليد أوامر cURL بنقرة واحدة، وحفظ تاريخ وسجل الطلبات مع حجب الـ Tokens الحساسة.

### 🐙 المرحلة 5: فاحص وأدوات مستودعات Git (Git Developer Tools)
- **فحص المستودعات:** استعراض حالة الفرع الحالي (Branch)، والمزامنة (Ahead/Behind)، والملفات المعدلة (Modified, Staged, Untracked).
- **عارض الفروقات (Diff Viewer):** خط Monospace Consolas مع تمييز لوني أسطر الإضافة (`+`) والحذف (`-`) لرؤوس التعديل.
- **توليد رسائل Commit (Conventional Commits):** توليد رسائل احترافية متوافقة مع معايير (`feat:`, `fix:`, `refactor:`, `perf:`, `docs:`, `test:`, `build:`, `ci:`) استناداً إلى طبيعة الملفات والفروقات.

### 🤖 المرحلة 6: منصة المساعد البرمجي بالذكاء الاصطناعي (AI Coding Platform)
- **تجريد المزودين (Multi-Provider Abstraction):** دعم متكامل لـ Google Gemini، OpenAI، الخوادم المحلية Ollama، ونقاط النهاية المتوافقة (OpenAI-Compatible).
- **13 إجراء برمجي متخصص:** شرح الأكواد، كشف الثغرات والأخطاء، إعادة الهيكلة، تحسين الأداء، توليد الاختبارات، كتابة Docstrings، إضافة Type Hints، فحص Traceback، توليد وشرح Regex و SQL، وتحويل الأكواد بين اللغات.
- **الخصوصية الصارمة (Privacy Context Preview):** نافذة معاينة إلزامية توضح بالضبط ما سيتم إرساله وحجمه والمزود المستهدف مع زر إرسال/إلغاء صريح، وحظر تام لأي إرسال تلقائي.
- **بث الردود (Token Streaming):** استقبال الردود كلمة بكلمة مع دعم الرجوع التلقائي (Fallback).

### 📁 المرحلة 7: إدارة ملفات المشاريع ومكتبة أوامر الطرفية (Projects & Commands)
- **التعرف التلقائي الذكي (Auto-Detection):** التعرف الفوري على لغة وإطار عمل المشروع (Django, FastAPI, Flask, React, Next.js, Vue, Docker) بمجرد اختيار المجلد دون مسح متكرر بطيء.
- **Active Project Context:** ربط المشروع النشط تلقائياً بأدوات Git ومكتبة الأوامر.
- **مكتبة الأوامر الآمنة:** مستودع مرجعي منظم لأوامر Git, Python, pip, npm, Docker, Linux, PowerShell مع دعم القوالب (`{{project_path}}`, `{{branch}}`, `{{port}}`) وبدون تنفيذ قسري مباشر لحماية النظام.

### 🔍 المرحلة 8: البحث الموحد الشامل (Unified Developer Search)
- **البحث عبر 10 مصادر محلية:** الاختصارات، الملاحظات، شات نوت، الحافظة، لقطات الشاشة، التعابير النمطية، طلبات API، المشاريع، وأوامر الطرفية.
- **خوارزمية الترتيب (Scored Ranking):** إعطاء الأولوية للتطابق التام لبداية الاسم والكلمات، مع تعزيز للعناصر المفضلة والحديثة.
- **التنقل المباشر الذكي (Direct Navigation):** عند النقر المزدوج يتم توجيه المستخدم تلقائياً إلى الصفحة أو الأداة المعنية وتفعيلها.

---

## 🛡️ 14. التثبيت الأمني الصارم ومراجعة الانحدار البرمجي (Final Hardening & Security Audit)

تم تطبيق إجراءات تثبيت أمني شاملة ومراجعة دقيقة لجودة الكود وتأمين البيانات:
- **تشفير المفاتيح والرموز الحساسة (Encrypted at Rest):** يتم تشفير مفاتيح API الخاصة بالذكاء الاصطناعي وطلبات API المحفوظة باستخدام خوارزمية Fernet مع مفاتيح مشتقة عبر PBKDF2-HMAC-SHA256 مقيدة بالجهاز وحساب المستخدم مع بادئة `enc:v1:` والترقية التلقائية للمفاتيح القديمة بدون فقدان للبيانات.
- **عزل التعابير النمطية والمهلة الحقيقية (ReDoS Isolation & True Timeout):** عزل فحص الـ Regex في عملية مستقلة (Subprocess) مع فرض مهلة زمنية إجبارية وقتل العملية فوراً عند تجاوز المهلة، مع تشغيل المطابقة في خيوط `RegexMatchWorker` خلفية لضمان عدم تجمد واجهة المستخدم إطلاقاً.
- **إلغاء آمن للعمليات البرمجية (Cooperative Cancellation):** استبعاد استخدام `QThread.terminate()` الخطرة واستبدالها بإشارات الإلغاء التعاونية (`is_cancelled`) وفك ربط الإشارات وإتاحة التنظيف التلقائي عبر `deleteLater`.
- **تنقية الروابط وسجلات التاريخ (Secret Sanitization):** تصفية واستبدال معلمات الاستعلام الحساسة (`api_key`, `token`, `secret`, `password`) بقيمة `[REDACTED]` قبل الحفظ في سجل التاريخ.
- **عمليات شبكة غير معطلة للواجهة (Non-Blocking AI Network):** نقل فحص الاتصال وقائمة النماذج إلى خيوط معالجة خلفية مع مؤشرات تحميل تفاعلية.
- **اختبارات شاملة وخلو تام من الأخطاء (128 Tests Suite):** اجتياز 128 اختباراً آلياً بنسبة 100% بدون أي أخطاء، واجتياز اختبار الدخان المرئي لشاشات الواجهة الرسومية (Smoke Test).

---

<div align="center">

**تم تطوير وتوثيق هذا المشروع ليكون الخيار الأقوى والأشمل للإنتاجية المكتبية.**
حقوق المشروع محفوظة © 2026 لمطور المشروع: [mahmoud-gala-dev](https://github.com/mahmoud-gala-dev/SnipGlide)

</div>


